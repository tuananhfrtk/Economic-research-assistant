# Base URL for the LLM API
# Groq provides fast inference for DeepSeek and other models
BASE_URL=
GROQ_API_KEY=

# API key for LLM services ( deepseek)
LLM_API_KEY=

# OpenAI key (needed for embedding models, but not for Groq)
# Only needed if using OpenAI embeddings
OPENAI_API_KEY=

#Huggingface model
HUGGINGFACE_API_KEY=

# Supabase settings (if needed for vector storage)
SUPABASE_URL=
SUPABASE_SERVICE_KEY=

# The reasoning model (DeepSeek via Groq)
REASONER_MODEL=

# The primary agent/coder model (can be another DeepSeek model or Qwen, etc.)
PRIMARY_MODEL=

# Embedding model (if using OpenAI or other providers)
EMBEDDING_MODEL=
